services:
  - type: web
    name: skynet-logistics-backend
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: alembic upgrade head && python -m app.seed && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
      - key: DATABASE_URL
        sync: false # Postgres externa (ej. Neon free) — completar a mano en el dashboard de Render
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: GPS_CREDENTIALS_ENCRYPTION_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: "http://localhost:5173"
      - key: SEED_SUPERADMIN_EMAIL
        value: admin@fleetops.dev
      - key: SEED_SUPERADMIN_PASSWORD
        generateValue: true
      - key: ROUTING_ENGINE
        value: fake
