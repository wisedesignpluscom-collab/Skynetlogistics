ENV=development

# Postgres
DATABASE_URL=postgresql+asyncpg://fleet:fleet_dev_pw@localhost:5432/fleet_db

# JWT
JWT_SECRET_KEY=change-me-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS
CORS_ORIGINS=http://localhost:5173

# Seed (usado por `python -m app.seed`)
SEED_SUPERADMIN_EMAIL=admin@fleetops.dev
SEED_SUPERADMIN_PASSWORD=change-me-please

# Encriptación de credenciales de proveedores GPS (Fase 3) — generar una nueva por entorno:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
GPS_CREDENTIALS_ENCRYPTION_KEY=JQXVPxHnSxbMkpnW7bNhD3WOFkrV6hYnId_QyerkDjw=
